TODO js (ECMAScript5):

write to browser history 
	for back, forward, bookmark
	
geolocation API

web workers (threads)

web messaging
	?

server push (server sent events)
	?
	see websockets

js
	JSON.parse
	arrays with foreach
	getter/setter
	
ISO-Datum, trim, bind, strict-mode,...