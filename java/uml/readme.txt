TODO:
UML-Diagrams:
	structure diagram 
		(Note: italics = abstract, circle for interface, 
		 +public,-private,#protected, underline=static
		 inherits = empty triangle, uses = arrow)
		class diagram
		object diagram
		package diagram
	behaviour diagram
		sequence diagram (including live-span)
			(Note for classes/objects: new - - > arrow
			 Note for method-calls:
			 synchronous full-arrow, asynchronous empty-arrow)
		
		